# 验证记录

验证日期：2026-09-16。环境：Windows PowerShell，Python 3.9.5。

实际执行：`powershell -NoProfile -ExecutionPolicy Bypass -File .\jiqirengailun\run.ps1`，工作目录为课程目录的上一级。进程退出码为 0。

## 示例验收

| 项目 | 实际结果 |
|---|---|
| 作业 1 网格路径 | up × 4，right × 4 |
| 作业 1 机器人指令 | turn_left，forward × 4，turn_right，forward × 4 |
| 两种执行的最终位置 | 均为 (4,4) |
| 最终机器人朝向 | E |
| 网格步数 / 机器人指令数 | 8 / 10 |
| 作业 2 最终位置 | (4,4) |
| 作业 2 环境步数 / 记忆步数 | 8 / 8 |
| 作业 2 轨迹位置数 | 9，包含起点 |

## 自动测试

作业 1：3 项测试通过。包含默认地图、空路径、目标不可达、非法输入，以及 4 个初始朝向 × 5³ 个三步动作组合，共 500 组独立执行位置对比。组合包括 stay、反向转身、边界碰撞和障碍碰撞。

作业 2：5 项测试通过。包含默认地图及记忆重放、起点即终点、不可达、执行上限与感知快照隔离、碰撞后的实际位置记录。

## 逐步地图功能复验

修改后再次运行 PowerShell 一键脚本，退出码为 0，原有 8 项自动测试全部通过。检查实际终端输出：初始地图 1 张，Step 1 至 Step 8 各 1 张，共 9 张；初始 A 位于 (0,0)，后续 S 保留在起点，* 随实际轨迹累积，4 个障碍始终显示为 #。第 8 步 A 位于 (4,4)，输出 goal reached。最后保留完整轨迹及 8 步统计，没有重复输出终点地图。

## 验证边界与原交付环境记录

PowerShell 一键脚本已实际运行；Bash 脚本已提供，但未在 Linux/macOS 环境实测。未进行真实机器人硬件测试或性能基准测试。当前系统找不到 git 命令，未创建 Git 仓库和提交历史。

## 第一轮算法优化复验

优化后再次执行 `run.ps1`，退出码为 0。作业 1 的 3 项测试通过；作业 2 原 5 项测试及新增的动态障碍重规划测试全部通过，共 6 项。两项作业总计 9 项测试。

默认地图仍沿原路径用 8 步到达 `(4,4)`，完整输出 9 张地图。智能体规划调用次数为 1，BFS 展开节点数为 19。动态障碍用例在首次行动后向原缓存路径加入障碍 `(0,4)`；智能体共规划 2 次，最终到达目标，实际轨迹不包含被阻断位置。

节点统计用于说明搜索工作量，未进行多轮计时基准；在 25 个格点的小地图上，不用单次毫秒耗时评价性能收益。

## 第二轮机器人指令优化复验

再次执行 `run.ps1`，退出码为 0。作业 1 增至 5 项测试，作业 2 保持 6 项测试，两项作业当前共 11 项测试，全部通过。

默认地图的基础流程保持不变：网格路径为 `up × 4 → right × 4`，翻译后包含 8 次 forward 和 2 次转向，共 10 条指令。朝向感知规划输出 `forward × 4 → turn_left → forward × 4`，包含 8 次 forward 和 1 次转向，共 9 条指令，最终位置 `(4,4)`、朝向 N，节省 1 条指令；搜索展开 54 个“位置＋朝向”状态。

默认场景从 `(0,0)` 到 `(4,4)` 至少需要 8 次前进，且初始朝向 E 时至少需要一次转向，因此任何可行方案不少于 9 条指令；实测方案达到该下界。边界测试还验证四种初始朝向均能到达目标、起点即终点返回空列表、封闭起点返回 None、非法朝向抛出 ValueError。
